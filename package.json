{
  "name": "my-bkper-app",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/web/client",
    "packages/web/server",
    "packages/events"
  ],
  "scripts": {
    "dev": "node /workspace/bkper-cli/lib/cli.js apps dev",
    "build": "node /workspace/bkper-cli/lib/cli.js apps build",
    "deploy": "bkper apps deploy",
    "deploy:dev": "bkper apps deploy --dev"
  }
}
